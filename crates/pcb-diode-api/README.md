# pcb-diode-api

`pcb-diode-api` contains the internal service clients used by the PCB CLI for
authentication, search, BOM matching, release uploads, routing, and remote
sandboxes.

Run the crate tests with:

```bash
cargo test -p pcb-diode-api
```
